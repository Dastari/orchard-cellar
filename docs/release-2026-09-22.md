# September 22 integrated release — 0.21.1

## Approved scope

The owner explicitly requested “merge everything then publish new version” after
reviewing open PRs 46–51. This authorizes their combined merge and production
release. No additional features, data migration or world-map publication is planned.

The integration branch `release/september-22-integration` retains the actual heads
of all six PRs, including the final PR47 documentation commit. Shared package
versions use the highest feature version; root 0.21.1 identifies the integrated
release. Both map-validation/resource-movement imports and interaction-footprint
imports remain in the authority. Renderer changes retain Studio actor artwork,
camera rounding, two-tile workbench scale and fruitless tree selection.

## Content and deployment

The reviewed content change is the workbench definition: native sprite scale,
two-cell collision/placement, carry capability and three-hit woodcutting salvage
of four planks. Capture the actual live content head and merge only reviewed
changes through content CAS. Inspect existing workbench right-hand cells before
publication. Preserve player/world rows and authored live definitions.

Verify full private schema and public bindings against the deployed immutable
module. If unchanged, use `world:release:routine` with retained code/static rollback,
matching game/Studio builds and same-owner reconnect parity. Keep the reviewed
Studio UI-kit guard. Publish no map edits and do not bypass any failed gate.

## Integration and validation

PRs 46–51 and integration PR52 merged through GitHub on 2026-09-22. The release
source is `2d13a3ee690148533dbd8ddbf54d3ace87a5e596`; every original feature head
is retained in its ancestry. Both integration CI runs passed. Local `npm run check`
passed 951 coverage files / 5,891 tests and 7 exhaustive files / 101 tests, plus
lint, typechecks, lifecycle integrity, content, world build and asset validation.
An independent integration review found no omitted feature or merge conflict.

The actual live module matched the previous immutable rollback artifact. Full
private schema and public binding comparisons passed. The reviewed content CAS
candidate changes only `object:workbench`: R15 / `2f704947` to R16 / `0f06c798`,
919 definitions, one upsert, no deletions or map publication. Candidate SHA256:
`3998145095e5276dbdc8c3f9c21e126e662a82544d0257818b1bb78daf452895`.

## Approved workbench relocation

Workbench 4133 in homestead space 10002 overlapped a chest when widened. The owner
explicitly approved moving it to a nearby clear spot. The guarded admin preview
and commit moved it from (67,54) to (67,53). Both destination cells passed server
validation; the adjacent chest and its contents compared unchanged before/after.
All six existing workbenches had clear right-hand cells after this operation.
No other world entity was moved.

## Dedicated publishing account

The owner explicitly approved persistent Admin access for
`dev@orchard.dastari.net`. The normal membership reducer applied the role, and
both an authenticated server query and the account's game browser verified Admin
plus the retained Content Editor grant. No Owner or Keycloak administration role
was granted. Future production releases still need their own user authorization.
See [the account runbook](agent-development-account.md).

## Publication evidence

The guarded routine release completed successfully on 2026-09-22 from source
`2d13a3ee690148533dbd8ddbf54d3ace87a5e596`. Release checks passed all 958 files /
5,992 tests, lint, typechecks, lifecycle/content checks, unchanged full private and
public schemas, reviewed Studio UI-kit guard, staged builds and static validation.
Non-destructive module publication and content CAS completed. Same-owner reconnect
parity passed across 42 tables, and public game/Studio HTML and asset bytes matched
installed output. All three runtime services are active.

- World module: `f907c31cd4fa93c4d6e0d3d09efb77bfffc70604baa8ec23c7fa61687a90a080`.
- Database identity unchanged: `c200af6ca3e4663bde9be65c18114d5d296f4b811bf0fa35d3771f8fdba89c21`.
- Content: R16 / `0f06c798`; published map remains R6.
- Game 0.19.0: `/assets/index-B7r1oLaN.js`, SHA256 `fb0ff1f8c278884744057807d2e1df5b23b619a8591d7ea83b26a4de3665bcbc`.
- Studio 0.11.0: `/assets/index-Cudjk-sh.js`, SHA256 `572e2d59f566100aff4636a554a0af1fbcf41a4882479e898726558c391a2962`.

The dedicated account reconnected after the update with Admin role, content ready,
content hash `0f06c798`, map revision 6 and no connection error. Empty hotbar slots
render cleanly. Studio's authenticated map canvas renders using the new bundle.
Expected transient connection errors occurred while the guard stopped web traffic;
post-release reconnect succeeded. No production map edits were published.

Private preflight evidence is in
`/home/toby/.local/state/orchard-release/september22-0211-preflight`.
Runtime checks, rollback module/static artifacts, reconnect snapshots, release log
and browser screenshots are in
`/home/toby/.local/state/orchard-release/september22-0211-routine`.
The installed Studio artifact is also recorded in `.git/cellar-ui-release.md`.
Temporary owner reconnect credentials, ciphertext and encryption keys were removed
after verification. The canonical checkout remains clean and detached at the
release commit; service and credential coordination is released at handoff.
