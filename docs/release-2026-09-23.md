# September 23 integrated runtime release — 0.27.2

## Deployment and acceptance status

The owner authorized merging and publishing the day's actual source PRs, followed
by player patch notes. GoldCondor coordinated publication from merged source
`78665a40b11ddc013decd02f7e5f8ead32e5b83f`. The guarded schema-only release
completed with exit code 0; world, game and Studio services are active.
**Browser acceptance remains incomplete.** A returning game browser retained old
service-worker atlas metadata (revision prefix `d6c8a7`) alongside a newer category
asset (`0044`), with a waiting worker. This startup incompatibility is under
investigation in a separate source PR. Successful module publication and reconnect
parity do not establish successful returning-player browser startup. Do not publish
player announcements or mark acceptance complete until GoldCondor records the fix
and fresh/returning browser results.

## Published scope and activation limits

The release integrates reviewed Studio editing improvements, structured content
forms and scoped permissions, asset/rule groundwork, sparse adaptive music,
progression authoring, object lifecycle state and appearance, shared workstation
and growth timing, and food/alchemy artwork. Content advances from R16 / `0f06c798`
to R17 / `0e741b0f`: 921 definitions in 27 kinds, 53 upserts and no deletions.
The three-way content audit found no conflicting custom live edits. Existing
progression values and historical cooking escrow recovery remain compatible.
No production map edits were part of this release.

D6 traversal policy remains **shadow**. Chunk runtime remains **default off**, with
optional shadow diagnostics only. No live chunk-authority switch, generator
retirement, chest/farm retirement, new hazard enforcement or food/alchemy gameplay
activation occurred. Artwork does not imply working new recipes, potions, hunting,
collecting or apiaries. The lifecycle hook bundle adds no newly authored interactions.
Player-facing notes must respect these limits; the coordinator's reviewed draft is
`/home/toby/.local/state/orchard-release/20260923-full/orchard-player-patch-notes.md`.

## Validation and data preservation

The final candidate passed 6,634 coverage tests and 101 exhaustive tests, plus
repository integrity, types, lint, content/assets and builds. Actual deployed and
candidate module bytes were used for complete private-schema and public-binding
audits: all 198 existing row schemas and named types are unchanged. Ten stored
tables and one view are additive; seven existing entity tables gain area indexes.
All 96 existing public rows retain visibility and all 102 existing private rows
remain private. Public additions are chunk heads, chunk shadow metadata and the
caller-filtered Studio scope view. Eight new private tables remain private.

The migration lane retained `--delete-data=never`, created and verified a fresh
quiesced backup, restored production data to an isolated authority, and published
the pinned candidate there before production publication. Reconnect comparison
passed for **one authorized account across 42 durable tables**, both in the restored
rehearsal and against the production result. This is account-scoped parity, not an
assertion that every player's state was independently sampled. Schema-only mode
performed no chest backfill, phase change or drain. Reviewed Studio guard, production
builds and static validators passed. Browser acceptance is tracked separately above.

## Evidence and recovery

- Release log, schema/content audits, content candidate, restored first/second
  snapshots and production snapshot:
  `/home/toby/.local/state/orchard-release/20260923-full`.
- Fresh verified data and matching rollback backup:
  `/home/toby/backups/orchard/20260923-full-release`.
- Previous retained backup:
  `/home/toby/backups/orchard/town-20260921-rubybay`.
- Pre-checkout rollback archive:
  `/home/toby/.local/state/orchard-release/20260923-full/pre-checkout-rollback.tar.gz`.
- Installed artifact/operational record: `.git/cellar-ui-release.md`, maintained
  by GoldCondor. Do not infer current artifact hashes from this source-only record.

Follow [publishing](../ops/orchard-runtime/PUBLISHING.md) and
[backup restoration](../ops/BACKUP-RESTORE.md) for recovery. Preserve database/module
mapping and matching static artifacts together; never publish mutable source merely
to roll back. Credential files and raw snapshots remain private and are not included
in this documentation.

## Authorized cleanup and proposed retention policy

Release preparation reclaimed approximately **33 GiB**: 22.46 GiB of approved old
backup data, 3.36 GiB from 14 audited worktrees, and 7.135 GiB from 133 regenerable
artifact directories. Worktree branch/remote refs were retained. Active/locked
worktrees, dirty work, unique artwork/evidence, current rollback artifacts, the
September 21 backup and fresh September 23 backup were preserved. Audit files are
`cleanup.json`, `cleanup-worktrees.json` and `cleanup-artifacts.json` in the release
evidence directory. Reclaimed sizes are approximate allocated disk measurements.

**Proposal for owner review, not a new automated policy:** retain the current and
previous verified backups with matching rollback evidence; review older copies for
explicit disposal only after restore and acceptance evidence is complete. At task
closeout, prune regenerable dependency/build staging after confirming no active
consumers, unique artifacts or rollback references. Keep source commits and concise
validation evidence. No pruning automation or scheduled deletion is introduced.

## Continuation handoff

GoldCondor retains sole release/service/credential ownership. First read this record,
`AGENTS.md`, the publishing runbook and current private release evidence; current
Git/service/artifact checks supersede this snapshot. Inspect `git status`,
`git rev-parse HEAD`, `gh pr list --state open`, and the startup-fix PR checks before
acting. Do not reuse or refresh another agent's rotating credentials.

Next: complete the returning-client startup fix through its own reviewed PR and
checks; let GoldCondor coordinate any additional publication. Verify a fresh game
session, an old cached tab's update/reload path, signed-in content readiness at
R17 / `0e741b0f`, and Studio's authenticated live map. Append exact browser/artifact
results here, then finalize player patch notes with only verified active behavior.
Retain the shadow defaults and existing durable-state/rollback gates. This record's
PR changes documentation only and must wait for its required CI before merge.
