Lane:        59-P2 bounded atlas builder
Goal:        Pack complete assets into <=512x2048 / <=4 MiB pages, preserving every original frame pixel and all 36 legacy PNG files.
Consumes:    docs/59 P2, docs/47 section 13 page-format items 1/2/3/5/7; integrator-owned reader contract: optional BuiltAssetRecord.pageId, new IDs `${category}:p000`, seasonal index keys `${pageId}:${season}`, filenames `atlas_${category}_p000_${season}.png`. Index v4, category v3, marker v2, registry v4. Retain readers for all supplied legacy versions (current writer actually emits registry v2).
Owns:        New packages/tools/src/assets/atlas-pages.ts and focused tests first. After explicit reader-merged message: packages/tools/src/build-atlas.ts, assets/types.ts, validate-assets.ts, builder/registry/pipeline tests, new page-validation helpers. New files <=400 lines.
Must not:    Never edit renderer.ts, overworld-main.ts, UI files, roadmap, doc59 ledger, DECISIONS.md or production dist. Do not touch root generated assets or the integrator's generated directory. Do not switch writers before the integrator confirms dual readers are committed. Do not add dependencies, content addressing, retention infrastructure or .omit output (P3 owns that).
Depends on:  P0 and P1 landed; P2 claimed by integrator. Writer switch additionally waits for committed dual readers.
Unblocks:    P3 builder variants and P8 legal page textures.
Done when:   First handoff: pure complete-asset packer rejects any asset that cannot fit a fresh page and tests every page/asset bound. Final handoff: one page/season RGBA buffer at a time, stable IDs, page-local frames, missing references/overlaps validated; npm exec vitest run relevant tools tests; original-vs-paged frame pixels byte-exact across all 36 atlases. Integrator performs npm run check before commits.
Hand-off:    Work only in /home/toby/projects/orchard-cellar-perf59-p2-builder on branch perf59-p2-builder. Do not commit before the integrator gate. Report changed paths, commands/results, packing counts and artifacts in output/perf-59-20260906/P2/. Preserve generated output in your own independent directory. Begin with pure packing module/tests; send that handoff before writer work.
