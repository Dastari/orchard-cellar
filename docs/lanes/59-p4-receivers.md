Lane: 59-P4 receiver coverage fields
Goal: Implement doc59 P4 static/moving split and O(1) rasterized receiver sampling while preserving doc58 pixels and owner exclusion.
Worktree: /home/toby/projects/orchard-cellar-perf59-p4-receivers
Consumes: docs59 P4; docs47 section1.3; docs58 decisions/receiver contract; current receiver-lighting/directional-shadows and review fixtures.
Owns: packages/engine/src/receiver-lighting.ts, directional-shadows.ts, receiver-lighting.test.ts, new modules/tests <=400 lines. Do not edit renderer.ts, overworld-main.ts, world-lighting-renderer.ts, roadmap, ledger, DECISIONS or other existing files. Report required cross-file type changes before making them.
Integrator owns: WorldLightingRenderer begin/static/moving wiring, main extraction, numeric owner construction, ImageData reuse and shared docs/commits.
Requirements: retain static coverage identity across moving changes; reusable working overlay; own-caster exclusion; rasterized ground sample within one 8-bit step of previous loop; fallback loop only for heights without raster plane; numeric keys/signatures in begin/sample; prepareHeights generation guard preserved. Reuse raster arrays on steady frame; no world readback or dependencies. Keep existing prepare API as a thin compatibility entry if fixtures need it, with explicit split API for integration. No duplicate dormant legacy engine.
Tests: literal P4 tests with600walking-equivalent frames/fixed sky; static coverage unchanged and zero rebuilds afterwarmup; moving overlay only movingcasters; sample comparison including owner exclusion; string-allocation test seam. Preserve per-frame counters; distinguish static rebuilds from moving blits.
Hand-off: explicit API/types, files, focused tests/typecheck/lint, quantitative accuracy/counter results under output/perf-59-20260906/P4/. No commits or generated asset changes. Do not run full check; integrator gates commits. If three approaches fail one requirement, report all three accurately rather than relaxing it.
