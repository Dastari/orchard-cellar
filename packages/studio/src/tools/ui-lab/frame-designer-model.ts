/** Compatibility names for existing Studio callers; the editing model and
 * retained preview now belong to the shared kit. */
export { createUiFrameDesignerModel as createFrameDesignerModel, UiFrameDesignerModel as FrameDesignerModel } from '@orchard/ui';
export type { UiFrameDesignerAccess as FrameDesignerAccess, UiFrameDesignerPublishRequest as FrameDesignerPublishRequest, UiFrameDesignerPublishAdapter as FrameDesignerPublishAdapter, UiFrameDesignerSnapshot as FrameDesignerSnapshot, UiFrameDesignerOptions as CreateFrameDesignerOptions } from '@orchard/ui';
