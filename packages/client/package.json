{
  "name": "@orchard/client",
  "version": "0.18.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "predev": "npm run ui:assets -w @orchard/tools -- client",
    "prebuild": "npm run ui:assets -w @orchard/tools -- client"
  },
  "dependencies": {
    "@orchard/lifecycle-authoring": "*",
    "@orchard/auth": "*",
    "@orchard/engine": "*",
    "@orchard/sim": "*",
    "@orchard/ui": "*",
    "@orchard/world-bindings": "*",
    "spacetimedb": "2.8.2"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
