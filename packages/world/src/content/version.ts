/** Bump only with a reviewed engine/content compatibility release. Kept in a
 * dependency-free module so offline release tooling cannot execute world code. */
export const CONTENT_ENGINE_VERSION = 1;
