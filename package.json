{
  "name": "orchard-cellar",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm run assets:build && concurrently -k -n client,server,assets -c green,blue,yellow \"npm run dev -w @orchard/client\" \"npm run dev -w @orchard/server\" \"npm run watch -w @orchard/tools\"",
    "build": "npm run assets:build && npm run build -w @orchard/sim && npm run build -w @orchard/tools && npm run build -w @orchard/client && npm run build -w @orchard/server",
    "check": "npm run typecheck && npm run lint && npm test && npm run assets:validate",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "test": "vitest run --coverage",
    "assets:validate": "npm run validate -w @orchard/tools",
    "assets:build": "npm run atlas -w @orchard/tools",
    "assets:render": "npm run render -w @orchard/tools --",
    "assets:preview": "npm run preview -w @orchard/tools",
    "assets:import": "npm run import -w @orchard/tools --",
    "sim:pace": "tsx packages/tools/src/pace-sim.ts"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitest/coverage-v8": "^4.1.11",
    "concurrently": "^10.0.5",
    "eslint": "^10.0.1",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0",
    "vitest": "^4.1.11"
  }
}
