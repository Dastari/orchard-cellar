<!-- date | area | decision | reason | supersedes -->
2026-08-24 | art/palette | use 55 closed palette colors indexed 0–54 | docs/10 §2 table contains 55 hex values despite saying 56; generating the table verbatim avoids inventing an unsigned color | —
2026-08-24 | art/terrain | adopt Sprout Lands Basic Pack as the single terrain reference/source, imported into palette-closed text grids | owner confirmed the paid pack is for a private non-commercial project and explicitly approved its use; this accelerates terrain while bespoke characters, trees, buildings, and UI preserve Orchard identity | docs/11 §6 public-pack default
2026-08-24 | art/review | allow M2 bootstrap anchors to use all available same-category anchors plus three style benchmarks when fewer than three approved neighbors exist | the first asset in a category cannot be rendered beside three pre-existing approved assets; generated review sheets record the available count and later assets enforce the full neighbor comparison | docs/10 §8 three-neighbor requirement during initial anchor bootstrap
2026-08-24 | sim/economy | fill the missing M4 workbench, yard, cellar, compost, and plot-clearing price table with stage-local geometric cost curves | docs/04 requires the table in docs/06 but docs/06 omitted it; M4 cannot implement upgrades or golden tests without authoritative values | —
2026-08-24 | sim/seasons | explicit per-species ×1.8 season traits override, rather than stack with, the general ×1.6 featured multiplier | docs/06 §2 states both values; treating the more-specific table entry as an override preserves both without a 2.88× accidental stack | —
2026-08-24 | sim/press-yard | set the pre-pipe yard jug rack capacity to 100 Must | docs/05 requires a visible storage-full backpressure state but omitted the threshold; 100 Must creates a meaningful early hauling cycle without changing the documented first-bottle window | —
2026-08-24 | roadmap/M4 | interpret M4's 30-minute completion check as accelerated browser exercise, while retaining the authoritative 45–70 minute unwarped first-bottle target | docs/14's former wording required a first bottle in 30 minutes "at" docs/06 §10 pacing, but docs/06 sets the window to 45–70 minutes; separating verification duration from simulated pacing makes both measurable | docs/14 M4 former Done-when sentence
2026-08-24 | sim/seasons | apply Spring's general ×1.6 Grove feature to every tree, let a currently matching named species trait use ×1.8, and use ×0.85 otherwise | docs/03 defines Spring as the whole Grove's featured stage while docs/06 gives named seasonal species a more-specific value; this implements both without stacking | 2026-08-24 sim/seasons
2026-08-24 | balance/cellar | raise the first Demijohn shelf cost from 40 to 80 Must | implementing Spring's binding ×1.6 Grove feature moved the measured first bottle from 48.17 to 40.83 minutes; this stage-local sink restores it to 45.83 minutes while first press remains 18 minutes | docs/06 former Demijohn cost
2026-08-24 | architecture/backend | pause the isolated-farm Fastify/SQLite backend and evaluate SpaceTimeDB 2.8 through a reversible M5.5 vertical slice | the owner expanded the target from small asynchronous visits to a friends-only persistent overworld where players walk between farms and farm together; this is a materially different realtime authority problem and the unbuilt backend is the cheapest place to validate it | docs/01 Fastify/SQLite stack, docs/02 FarmRoom architecture, docs/07 asymmetric-visiting scope, docs/08 SQLite authority, docs/09 password-session plan
2026-08-24 | architecture/backend | adopt SpaceTimeDB 2.8 as the sole world authority and durable store | all ten M5.5 checks passed: shared predicted movement, spatial handover, reducer authority, atomic contention, private isolation, durable reconnect/restart, one-command local boot, and shared-sim replay; keeping the unused Fastify/SQLite path would create conflicting authorities | 2026-08-24 architecture/backend evaluation gate
2026-08-24 | tooling/typescript | disable exactOptionalPropertyTypes and declaration emit in packages/client only | SpaceTimeDB CLI 2.8.2 generated bindings do not typecheck with exact optional properties and declaration portability, while handwritten client code remains strict and sim/world retain their stricter settings | docs/15 strict TypeScript default for client generated boundary
2026-08-24 | roadmap/multiplayer | pull a narrow M7a farm loop forward as M5.6 before completing the M5 progression rules | the owner explicitly prioritized a tangible friends-MMO sample while a separate agent prepares the Cute Fantasy world tiles; adjacent persistent parcels and one cooperative crop transaction validate the game shape without committing to the full permission/economy design | docs/14 strict M5-before-M7 ordering for this vertical sample only
2026-08-24 | art/licensing | adopt the owner-purchased Cute Fantasy packs as the primary world and UI source for this private build, committing only reviewed palette-authored derivatives and never the purchased source sheets | the owner explicitly approved using the paid packs and confirmed the project is private; each included pack license permits commercial and non-commercial project use and modification while prohibiting redistribution or resale of the source assets | docs/11 former one-pack terrain-only restriction; docs/18 former Cute Fantasy study-only rule
2026-08-24 | world/design | replace the authored road-grid overworld target with a deterministic shared survival island and pull its first playable slice forward as M5.7 | the owner prioritized exploration, biome generation, resource gathering, collision, inventory, and per-player wilderness starts before deeper farming rules; pure shared generation preserves client/server agreement while SpaceTimeDB stores only mutable state | docs/07 authored-overworld assumption and docs/14 strict M6-before-M7 order for this vertical slice
2026-08-24 | art/palette | preserve reviewed Cute Fantasy extracts with validated per-asset native RGB ramps instead of snapping them to Orchard's closed bespoke palette | the owner identified that palette snapping visibly muted the purchased pack's saturated greens, warm paths, water, and soft layered ramps; text-grid geometry and binary alpha remain authoritative while `sourcePalette` restores intended source colors | docs/10 and docs/11 global closed-palette rule for all assets
2026-08-24 | art/licensing | correct the Cute Fantasy license record: paid packs allow commercial projects, while Cute Fantasy Free and its current farmer avatar are non-commercial-only | the earlier umbrella statement treated every `Cute_Fantasy*` folder as a paid pack; the bundled Free readme has narrower terms, which still permit the owner's explicitly private non-commercial use | 2026-08-24 art/licensing umbrella statement
2026-08-24 | art/trees | use the reviewed Cute Fantasy fruit-tree source stages at their native 16×32 sapling and 32×32 young sizes | the earlier canonical 16×16/16×32 sizes clipped or blanked source pixels; retaining the native crop preserves the purchased pack exactly and keeps every stage on the 16 px world grid | docs/10 §5 former sapling and young sizes
2026-08-24 | world/generation | advance the survival island to version 2 with resource-free inter-clearing trails and 22% maximum forest-tree occupancy | version 1's 38% forest occupancy formed near-solid barriers around some starts and made authoritative reconciliation feel like an invisible movement limit; guaranteed trails preserve exploration while keeping forest regions visibly dense | docs/20 version 1 resource distribution
2026-08-24 | world/generation | widen survival trails to include both outer edge tiles and advance the island to version 3 | live collision-overlay walking found that the lower grid boundary clipped tile 47 from an otherwise three-tile corridor, allowing a tree base to block a player who entered slightly left of center | 2026-08-24 world/generation version 2 trail bounds
2026-08-24 | client/controls | define source render scale 2 as user-facing 1×, make wheel cycle the nine slots, reserve +/- for world zoom and Shift +/- for whole-pixel HUD scale, and remove the F fullscreen key | owner corrected the intended zoom perception and interaction bindings after exercising the live survival island; double-click preserves discoverable fullscreen without occupying a gameplay key | docs/20 §5 former absolute 1×/2×/3× labels and F binding
2026-08-24 | client/movement | use an 8×6 px foot collision box and cardinal side-pose fallback for diagonal travel | the owner could not move as far behind tree canopies as the art implied and correctly identified that northwest/northeast were displaying straight-up poses; the licensed sheet contains cardinal poses only | docs/20 §3 former implicit player body and §5 animation behavior
2026-08-24 | client/movement | terminate the 8×6 px body box at the avatar's authoritative foot point and anchor the licensed crop on its actual last shoe row | live collision inspection showed the crop retained nine transparent rows below its shoes, making a centered body box appear a tile beneath the character | 2026-08-24 client/movement centered foot collision
2026-08-24 | world/collision | represent live tree trunks as shared 8×6 px fixed-point obstacles instead of blocking their entire 16×16 resource tile | live play still felt like authority pushback behind canopies after the player anchor was corrected; the full-tile resource blocker was materially wider and taller than the visible trunk base | docs/20 §3 full-tile live tree blockers
2026-08-24 | client/controls | bind E to pickup, F to selected-tool use, Q to drop, and restore mouse-wheel world zoom while scaling player name tags with UI chrome | owner revised the survival interaction layout after live play; these bindings reserve pickup as the primary nearby-world interaction and keep tool use explicit | 2026-08-24 client/controls wheel inventory selection and E tool use
2026-08-24 | world/items | replace direct survival Wood rewards with public authoritative ground-item rows and keep usable Wood exclusively in the private nine-slot inventory | the owner specified a Minecraft-style hotbar, visible dropped logs, cross-player pickup, and no duplicate top-level resource counter; server-derived placement and atomic pickup preserve MMO ownership and race safety | docs/20 §4 direct player_survival Wood grant
2026-08-24 | art/characters | retain licensed tool-action frames on their native 32×32 cells with their 15,22 body-foot anchor | the Axe sweep crosses the 16 px body boundary and the actor is centered near x=15 in action cells; importing/anchoring only the left half clipped the effect and made the down-facing pose jump right | docs/10 canonical 16×32 character canvas for every frame
2026-08-24 | art/transparency | preserve nonzero native alpha in reviewed licensed imports through per-asset RGBA palette entries | Cute Fantasy tree sheets author their 113-pixel ground shadows at alpha 40; the former binary threshold deleted those source pixels and visibly detached trees from the ground | docs/10 and docs/11 binary transparency rule for licensed imports
2026-08-24 | client/scenes | retire the solo farm scene and make the shared overworld the sole game client until farms return as renderer-backed instances | maintaining the obsolete scene stack duplicated canvas, font, save, and rendering paths while the owner is validating the MMO overworld | docs/02 scene stack until instanced farms
2026-08-24 | client/rendering | use continuous 0.25-step sharp-bilinear world zoom with source zoom 2 labelled 1x | an integer nearest-neighbour sharp pass plus one eased smooth final blit keeps integer stops crisp and removes fractional shimmer across DPR values | docs/20 three stepped render scales
2026-08-24 | client/rendering | retain Canvas 2D behind one unified renderer module after evaluating WebGL2 | the target GT730 supports WebGL2 but the required chunking, lighting, particles, and budgets are met by Canvas 2D; renderer ownership preserves a later backend swap path without touching game logic | docs/21 WebGL evaluation gate
2026-08-24 | client/terrain | unify chunk rendering, lighting, diagnostics, and immutable collision against one cached terrain byte array per seed/version | one classification source prevents visual/collision disagreement and removes full-island collision rebuilds at subscription boundaries | per-frame procedural ground and resource-triggered terrain rebuilds
2026-08-24 | client/nameplates | render player nameplates post-light inside the world composite with a compact translucent backing | DOM/UI-space remote labels jittered during camera motion and did not scale consistently with the avatar; sharing the world transform fixes both while post-light placement keeps names readable | docs/21 screen-space name tags
2026-08-24 | client/lighting | preserve nearest-neighbour tile lightmap regions instead of smoothing the multiply pass | the owner preferred visibly pixelated day/night and point-light falloff that matches the world art | docs/21 soft lightmap sampling
2026-08-24 | client/weather | use exact licensed diagonal rain and seven-frame impacts in camera-relative pooled emitters whose visuals scale with world zoom | owner review required exact splash endpoints/full animation, rain that does not follow the player like a local cloud, and smaller weather relative to terrain when zoomed out; the emitter boundary remains compatible with later cloud-scoped weather | docs/21 procedural fixed-screen-size rain proof
2026-08-24 | client/weather | depth-sort each complete rain streak and splash by its ground-impact foot Y against one mandatory queue for every non-ground world drawable | weather must pass correctly behind or in front of trees, people, items, props, houses, and buildings rather than compositing as an always-front screen overlay; impact depth gives the expected 2D world ordering without per-pixel occlusion | docs/21 post-light always-front weather layer
2026-08-24 | client/netcode | replace position error blending with authoritative rebase plus bounded input replay | blending toward an RTT-old row created permanent drag, while deterministic replay through shared fixed-point movement reaches zero steady-state correction | docs/02 soft reconciliation
2026-08-24 | client/netcode | render remote avatars from ten-snapshot buffers at a 1.5-authority-tick delay with two-tick collision-clamped extrapolation | a frame-rate-dependent divide-by-three filter stuttered, parked off target, and leaked departed player state; a timed buffer gives constant-velocity motion and bounded stalls | docs/19 unspecified remote interpolation
2026-08-24 | world/netcode | refresh held input every 20 client ticks, retry one reducer failure immediately, and ignore authority input stale for more than two seconds | edge-only input could leave a rejected or crashed client walking for the full 30-second presence lease; movement liveness and online presence need independent bounds | docs/19 heartbeat-only movement liveness
2026-08-24 | client/interactions | predict cosmetic interaction feedback but keep resource, inventory, item, and pickup state exclusively authoritative | immediate swings and pickup/drop poses improve feel without creating a second state authority or weakening atomic anti-duplication transactions | bare round-trip interaction feedback
2026-08-24 | world/netcode | use player_position.authorityTick as the sole replicated interpolation timeline | the scheduled 20 Hz counter is deterministic, transaction-aligned, and rate-agnostic; wall-clock timestamps in public movement rows would mix transport and simulation time | implicit row-arrival timing
2026-08-24 | client/animation | use exactly two avatar channels—display-displacement-derived locomotion and registry-driven replicated actions—and mirror equippedKind publicly on player_position | observers need held tools and transaction-aligned one-shots, while per-avatar atlas-timed controllers avoid bespoke network fields and global magic frame clocks | local-only axe animation and player_equipment lookup
2026-08-24 | world/movement | account movement as client-tick runs settled against server-clamped elapsed-time credit | 20 Hz direction sampling silently lost short taps and disagreed at every transition; capped catch-up preserves honest 60 Hz steps without trusting raw client time or allowing speed hacks | sampled three-current-direction-steps per authority tick
2026-08-24 | world/schema | defer removal of world_tree, tendTree, and the v1 connection_presence table to a separate compatibility cleanup | the netcode migration is additive and must not mix transport correctness with deletion of durable vestigial M5.5 data | —
2026-08-24 | world/movement | keep same-direction input refreshes liveness-only and settle runs only on direction transitions | closing a run on a refresh can count client steps already due in the next authority tick, producing a repeatable three-pixel correction every refresh; transitions are the unambiguous run boundary | docs/22 same-direction incremental settlement
2026-08-24 | client/netcode | snap the interpolated render clock only when authority time differs by more than ten ticks | normal drift remains softly corrected, but backgrounding or reconnecting can otherwise leave an expired one-shot frozen on its opening frame while a timeline thousands of ticks behind catches up | docs/22 never-jump render timeline
2026-08-24 | client/animation | resolve semantic action groups first and retain an explicit legacy alias for the licensed farmer axe rows | the purchased sheet predates the replicated `swing_axe_<facing>` convention and already ships reviewed `axe_<facing>` groups; the alias avoids duplicating or re-importing art while missing groups still use the generic fallback | docs/22 semantic-only group lookup
2026-08-24 | world/netcode | confirm movement every three client ticks and atomically commit complete credited batches before advancing their acknowledgement | speculative open-run movement accumulated small overshoots on repeated taps, while partial batch rows caused replay duplication under jitter; confirmed intervals preserve exact paths without weakening the server-time rate cap | 2026-08-24 world/movement run settlement; 2026-08-24 world/movement liveness-only refreshes; docs/22 20-tick refresh
2026-08-24 | client/netcode | serialize latency-injected outgoing dispatches and incoming transaction groups independently of timer resolution | 150 ± 50 ms jitter can coalesce sub-millisecond timer targets, and time ordering alone did not guarantee reducer or table-commit callback order | implicit timer ordering in the doc/22 latency harness
2026-08-24 | client/performance | bound deferred position and RTT stores, prune region-scoped resource effects, and grow the offscreen world pass in coarse buckets | background tabs could accumulate work indefinitely and eased zoom reallocated a multi-megabyte canvas on successive frames | unbounded callback/cache queues and exact-size world-pass allocation
2026-08-24 | world/equipment | use `player_position.equippedKind` as the sole runtime public equipment mirror while retaining the legacy table inert for durable-schema compatibility | manually synchronizing two public equipment rows across reducers invited drift; additive SpaceTimeDB migration policy defers destructive table removal | dual writes to player_position and player_equipment
2026-08-24 | client/actions | replicate selected-tool action use even when it misses, while keeping target mutation validated and atomic when an id is present | observers should see another player swing an Axe regardless of whether the swing intersects a tree | successful-harvest-only action replication
2026-08-24 | client/time-audio | derive HUD, lighting, rain, and music from shared authority-to-sim day conversion and select the authored night theme after dusk | treating a 20 Hz authority tick as a 60 Hz sim tick stretched the documented day and let consumers drift; the night song was otherwise unreachable | scattered tick-of-day conversions and spring-only overworld music
2026-08-24 | client/entrypoint | use `/` and `index.html` as the sole account/game application entry and remove the legacy account and overworld HTML pages | the owner requested a conventional Vite application without page-specific URLs; `src/main.ts` can select the view while preserving one origin and callback | docs/02 one-game-client entry and docs/21 farm-scene removal
2026-08-24 | auth/provider | target a self-hosted OIDC provider for production accounts, with Keycloak as the implementation baseline pending the documented gate | the owner wants locally controlled account creation/login without depending on managed SpacetimeAuth; standards-based OIDC preserves SpaceTimeDB identity validation and avoids custom password handling | docs/09 SpacetimeAuth-first integration
2026-08-25 | auth/provider | OPEN: approve Keycloak 26.7.2 amd64 digest eb81d22b… and PostgreSQL 17.11-alpine3.24 amd64 digest 7456ef82… plus the CT 105 operational footprint | doc 24 requires explicit owner approval before the prepared Compose stack may be installed or started | —
2026-08-25 | auth/identity | OPEN: declare existing host-issued development identities disposable or require a separately reviewed proof-of-both-identities migration | issuer and subject determine a new SpaceTimeDB identity, and ownership must never move by email or display name | —
2026-08-25 | auth/backup | OPEN: approve an off-machine authentication/SpaceTimeDB backup destination and retention policy | permission-restricted local pre-change backups exist, but local storage does not satisfy disaster recovery | —
2026-08-25 | auth/edge | OPEN: create auth.orchard.dastari.net DNS, provide SMTP relay values, and verify the NPM host key out of band | public discovery, recovery mail, NPM backups, HAProxy/SNI changes, certificate issuance, and live acceptance cannot safely proceed without these external inputs | —
2026-08-25 | auth/provider | APPROVED: deploy Keycloak 26.7.2 amd64 digest eb81d22b… and PostgreSQL 17.11-alpine3.24 amd64 digest 7456ef82… with the documented CT 105 footprint | the owner explicitly approved deployment after confirming the private HTTPS NPM upstream and SMTP configuration | 2026-08-25 auth/provider OPEN entry
2026-08-25 | auth/backup | APPROVED: encrypted SMB 3.1.1 backup target `//10.0.1.190/backups/orchard` with the documented 30-day retention | the dedicated NAS account authenticated successfully and an uploaded then downloaded pre-OIDC backup matched every source SHA-256 hash | 2026-08-25 auth/backup OPEN entry
2026-08-25 | auth/identity | APPROVED: discard existing host-issued development identities and establish production ownership from the exact captured Keycloak-backed SpaceTimeDB identity | development identities carry no production ownership that must be migrated; no email-, username-, or display-name-based ownership transfer is permitted | 2026-08-25 auth/identity OPEN entry
2026-08-25 | auth/edge | RESOLVED: publish `auth.orchard.dastari.net` through the backed-up NPM/HAProxy edge to the private TLS listener at `10.0.1.150:8443`, with SMTP verification/recovery enabled | DNS, certificate, exact-origin discovery/JWKS checks, upstream certificate verification, redacted access logging, and delivered account mail all passed | 2026-08-25 auth/edge OPEN entry
2026-08-25 | auth/authorization | keep Keycloak administration and Orchard game administration as separate permission planes; the exact captured production game identity is a SpaceTimeDB `owner` | SpaceTimeDB can recheck and revoke current membership in every reducer, while a role copied into an OIDC token would be stale until token renewal and would duplicate authorization state | —
2026-08-25 | world/admin | persist shared calendar and weather in SpaceTimeDB and reserve their audited reducers for the `owner` membership role | date/time/weather changes must affect every client and survive reconnects; a browser-only override was neither authoritative nor access-controlled | —
2026-08-25 | auth/admission | automatically provision every valid first-time Orchard OIDC identity as an audited `friend`, while keeping privileged roles, revocation, and blocking in SpaceTimeDB | the owner opened verified self-registration to gameplay; issuer/audience validation and Keycloak email verification remain the account gate, and registration must never grant developer controls | docs/09 friends allowlist and docs/24 unapproved-user rejection
2026-08-25 | design/stats | adopt docs/25: six hidden D&D attributes (base 10), derived Health/Mana/Vigour vitals, and a single flat→pctAdd→pctMult→override modifier pipeline with exclusive families | one owner-directed stat system that tools, effects, equipment, skill trees, and future combat all plug into as data instead of parallel math | —
2026-08-25 | design/stats | map Health to Strength and Vigour to Constitution (Mana to Intelligence), deviating from tabletop D&D's CON-based HP | owner direction: STR is the body stat, CON the endurance stat that paces tool use; recorded so it is not "corrected" later | —
2026-08-25 | design/stats | health reaching 0 causes a knockout (respawn at spawn slot, 25% health, winded debuff, never item or currency loss), amending the docs/06 §10 no-fail-states invariant | vitals and future combat need a real floor, but the cozy-game promise that setbacks never destroy progress stays binding | docs/06 §10 "no fail states" absolute
2026-08-25 | design/stats | reuse the name "Vigour" for the new player tool-pacing resource while the retired farm scene's tend-charge meter keeps the name in legacy code | same diegetic idea promoted to a first-class character resource; the legacy scene is retired so players only ever see one Vigour | —
2026-08-25 | design/tools | supersede docs/28's no-durability rule with per-item tool durability; tools become unusable but are never destroyed, and one renewable Wood or Stone repairs a selected tool to full | the owner explicitly added durability bars to the stats/vitals rollout; renewable surface pickups preserve the cozy no-softlock invariant | docs/28 §13 no-durability rule
2026-08-25 | design/spaces | adopt docs/26: separate areas are a spaceId dimension (u8 column + per-space local coordinates and seeds) on the existing SpaceTimeDB tables, not coordinate-band offsets and not a second module | SpaceTimeDB has no native map concept; one explicit dimension serves the mines and every future interior while keeping single-module auth, views, and subscriptions | —
2026-08-25 | design/mines | underground mine levels are shared-persistent and seed-deterministic (regenerated only by explicit version bumps), never per-visit or per-player random | the whole architecture rests on client and authority generating identical terrain from the seed, and co-op mining in one shared world is a social pillar | —
2026-08-25 | design/mines | mine darkness uses fixed per-space ambient below the surface comfort floor; torches never burn out, entering without light is permitted, and in v1 your light source occupies your active slot so lighting and mining are mutually exclusive | darkness is atmosphere and rhythm, not a fuel tax or a fail state; the hold-a-torch-or-swing tension is the mine's core loop and composes with Vigour pacing | —
2026-08-25 | design/lighting | adopt docs/27: blocky tile-quantized lighting is the binding art direction — one texel per tile, banded falloff, and a ban on soft gradients including docs/21's optional radial additive glow | the owner chose the pixel-stepped light feel as the style itself, so smoothness is a regression, not an upgrade | docs/21 §5 optional soft glow
2026-08-25 | design/lighting | light occlusion is a tile-fidelity BFS flood over the shared terrain classification with path-distance falloff; walls block, doorways and portals spill light, and sub-tile objects (trees, props, players) never occlude | docs/21 sanctioned flood-not-raycaster, path distance gives dimmed around-the-corner spill for free, and sub-tile shadows are below the system's one-texel fidelity | docs/21 §5 occlusion deferral
2026-08-25 | design/lighting | all light colors come from the single capped LIGHT_COLORS table (minimum channel 64, no full saturation); flicker jitters only radius and strength via deterministic time-slot hashing, never color or position | multiply compositing with saturated light crushes sprite channels to unreadable, and constrained flicker reads as flame rather than strobe | —
2026-08-25 | design/crafting | adopt docs/28: Minecraft-style crafting through the single 3x3 grid with proximity-gated stations (workbench/anvil/campfire stateless, furnace the one stateful window), content strictly limited to licensed paid-pack art | one grid UI keeps the shipped reducer/window model while placed stations still create progression beats; the art ceiling keeps every authored item shippable | docs/23 "recipes are a later milestone"
2026-08-25 | design/crafting | "workbench" names the placeable crafting station from now on; docs/06's fruit-currency "workbench upgrades" shop is renamed the farmhouse upgrade counter | the two docs used one word for unrelated systems and doc 26 already promised crafting "at the workbench" | docs/06 workbench naming
2026-08-25 | design/crafting | wood and planks are the smelting fuel economy (no coal ore is added) and plant fiber enters via deterministic hash-gated drops from tilling wild grass | the eight existing ore kinds stay the full mineral set, chopping gains a permanent sink, and fiber closes the string gap for torch/bow/rod recipes without adding creatures or new resources | —
2026-08-25 | design/crafting | shaped recipe matching becomes shift-invariant over the pattern bounding box, without mirroring | the shipped matcher anchors patterns at grid index 0 so any sub-3x3 recipe would only match top-left, which is unusable for authored content | —
2026-08-25 | world/terrain | render coast and biome boundaries as layered eight-neighbour autotiles, smooth the generated island mask, and restrict south-projecting coastal cliff art to south-facing shores | the licensed sheets include unused inverse corners and grass/savanna fringes; semantic masks preserve coves while eliminating hard biome seams and perspective-invalid side-coast walls | cardinal-only shoreline frames and per-cell coastal ruggedness
2026-08-25 | world/terrain | do not derive coastal cliffs from the coast-depth/beach band; generate them only from a coherent elevation contour through the shared layered-cliff resolver | a noisy shoreline band is not a raised shape and exposes disconnected rock dashes once terrain-fill colours are correctly transparent | south-facing filtering of per-cell coastal ruggedness
2026-08-25 | world/scale | centre the unchanged 320×320 seeded island in an 832×832 surface with 256 ocean tiles on every side and migrate persistent spatial rows by the same offset | this creates meaningful future boat/island space without reseeding the island or detaching players, NPCs, farms, soil, crops, chests, items, or hives from their existing locations | 320×320 world bounds
2026-08-25 | world/collision | resolve surface collision through shared ground, water, and air traversal media; calm water is open only to water actors, shore/land/waterfalls block them, and authored water rocks are physical water obstacles | swimmers previously bypassed collision and stayed in ponds only through habitat checks, which could not support rocks or future boats consistently | entity-specific collision bypasses
2026-08-25 | world/items | keep Minecraft-style multi-slot gestures client-responsive through a live pure-rule preview but commit each gesture once in an authoritative reducer transaction; retain otherwise unsafe transient items in a private per-player overflow ledger that drains automatically | repeated per-hover network writes can race stale snapshots and duplicate or lose stacks, while dropping crafting overflow is not a durable custody guarantee | incremental network mutation for shift-drag and best-effort ground overflow
2026-08-25 | world/chests | preserve placed chests as authoritative 27-slot entities, render the licensed Chest_Anim open/close frames, and break them after three axe hits by atomically spilling the chest item and every stored stack | this gives storage a complete interaction lifecycle without serializing contents into a carried inventory item or risking partial destruction | static chest art and indestructible placed containers
2026-08-25 | design/worldgen | adopt docs/30: semi-infinite chunked generation — pure seed-deterministic terrain sampled unbounded on both sides, lazy server materialization of entity rows per 16-tile chunk, extent ±32k tiles on the existing i16 columns | the fixed 320-tile island cannot host continents, deserts, mountains, or volcano islands, and both the chunk indexes and the never-stream-terrain invariant already support the infinite model | docs/20 fixed-island generator
2026-08-25 | design/worldgen | biomes are selected from a quantized multi-noise table (continentalness/erosion/ridges/temperature/moisture) whose cell adjacency makes transitions smooth by construction | border smoothness enforced by table topology needs no blend passes and structurally forbids jarring pairings like desert against cold biomes | —
2026-08-25 | design/worldgen | hand-crafted curation composes over procedural generation: terrain overrides beat the sampler everywhere, curated or player-modified chunks never regenerate on version bumps, and .map.json becomes the owner stamp/prefab format | the owner must be able to edit terrain and place entities after the fact without generator upgrades destroying that work | —
2026-08-25 | design/worldgen | migrate to world v2 by re-anchoring the legacy island at the origin as the first curated region, replaying its deterministic terrain into overrides and preserving every player-built row | the home island and player state survive pixel-perfect while the infinite world opens beyond its old coastline | —
2026-08-25 | design/wildlife | wildlife spawning becomes biome-weighted with per-biome color-variant pools, herd-clustered at chunk materialization with density caps and a lazy repopulation sweep | fauna should read as native to its biome (vultures mostly in deserts, dark herds in savanna) using the variant sheets already committed and the shipped wildlife species system | wildlife island-wide uniform placement
2026-08-26 | world/commerce | store gold/silver/bronze as one private per-character u64 bronze balance and require inventory plus wallet changes to commit in the same authority transaction | coins are a stat rather than inventory items, canonical smallest-unit storage avoids denomination drift, and atomic preflight prevents loss or duplication | docs/06 retired-scene currencies are not overworld inventory money
2026-08-26 | world/dialogue | drive NPC conversations from a shared data registry while persisting only the caller's active NPC/dialogue/node row | future NPCs can reuse mouse/1–4 branching dialogue and shop presentation without bespoke windows, while the authority validates every transition and transaction | client-owned dialogue progression
2026-08-26 | design/combat | adopt docs/32: combat is opt-in danger-zone content (mines, volcano islands, authored stamps) targeting hostiles only — no PvP, wildlife untargetable in v1, knockout remains the only defeat state | the cozy pillar survives a combat era when danger is a place you choose to enter and losing never destroys progress | docs/25 Phase 5 approval gate
2026-08-26 | design/combat | the world authority stays at 20 Hz through the combat era | 350-500 ms swing intervals, cone melee, and the shipped swept projectile stepping all resolve cleanly inside 50 ms ticks, so the deferred rate increase buys nothing | docs/22 combat-era rate deferral
2026-08-26 | design/combat | damage is deterministic and stat-driven through the docs/25 modifier pipeline, with variance and crits from the stateless checks hash — no stored RNG anywhere in combat | one integer math path serves players and creatures both directions and stays replayable in tests | —
2026-08-26 | design/combat | standing in light doubles hostile detection range in dark spaces relative to skulking in darkness | the mines' hold-light-or-swing tension becomes tactical: light is safety from stumbling but exposure to the things that watch | —
2026-08-26 | world/statistics | store lifetime player statistics as private typed kind-plus-subject u64 rows with immutable milestone crossings, and require every future authority-owned gameplay feature to register its outcomes | a narrow stable schema can retain detailed per-item/resource/creature facts while the sim registry prevents typo keys and documentation drift; caller views preserve privacy until an opt-in social design exists | —
2026-08-26 | design/lighting | revise the lighting charter to quarter-tile texels (4 px grain) with 10-band quantized falloff and a quantized additive ember-halo pass; smooth gradients stay banned | the owner raised the fidelity target against Core-Keeper-style reference shots — pooling and cones need sub-tile grain while remaining visibly pixelated | 2026-08-25 design/lighting one-texel-per-tile charter
2026-08-26 | design/lighting | solid objects become soft attenuators in the light flood (55% per texel) so furniture and trunks cast readable shadows, while walls and cliff faces stay hard blockers and players/NPCs still never occlude | the reference shots' light-between-obstacles look needs partial occlusion, and moving-entity flood shadows would shimmer | 2026-08-25 design/lighting sub-tile objects never occlude
2026-08-26 | design/lighting | terrain exports an integer terrainHeightAt(x, y) contract (docs/30 elevation; the plateau generator until it lands) consumed for light containment across cliff levels, rim spill, and banded cliff drop-shadows | 2D top-down still has a height story, and lighting must read one shared field rather than re-deriving elevation | —
2026-08-26 | design/lighting | global light events (deterministic lightning with a mandatory reduceFlashes cap, portal rays, canopy sun shafts) and primitive skewed-silhouette directional shadows driven by a clock-derived sun azimuth are the sanctioned dynamic layer | sky drama and grounded objects come from pure functions of the authority tick — identical on every client with zero netcode | —
2026-08-26 | backend/scalability | adopt docs/34: the vast-world hardening plan — docs/30 phase 2 is gated behind its stages 1-2, and stage 3 makes full-table iteration inside scheduled tick reducers a banned pattern | the audit measured 72% of the 50 ms tick budget spent on full-scan collision at today's world size, and every blocker has a named SpaceTimeDB feature (Range scans, RLS, one-shot and multi-cadence schedules) as its fix | —
2026-08-26 | backend/scalability | subscription cost is bounded by a hard client-side MAX_VIEW_RADIUS constant with rectangular viewport-derived bounds and chunk-crossing hysteresis, never by world size | the current clamp scales with world dimensions and would permit a 64-million-query subscription set at the docs/30 extent | —
2026-08-26 | backend/scalability | the single 20 Hz stepWorld splits into multiple scheduled reducers by cadence (20 Hz movement/projectiles, 1 Hz regen/hives/repopulation, slow migrations/trims), and world_item ground drops gain expiry | one reducer carrying every concern makes slow work (a measured 3.1 s first wildlife generation) stall the movement tick, and unexpired drops grow unboundedly | —
2026-08-26 | auth/theme | make account entry feel in-game through separate Sign In, Create Account, and Recover Account actions plus a read-only CSS/art Keycloak theme; credential forms and all OIDC state remain provider-owned | this preserves Keycloak's CSRF, recovery, verification, password-manager, accessibility, and future OTP/provider flows while matching the Orchard UI and retaining the canonical issuer and existing identities | embedding or recreating password forms inside the canvas client
2026-08-26 | design/homesteads | adopt docs/35: player farms are per-player instanced spaces on the docs/26 model with spaceId widened to u16, established on the shared overworld as real farmhouse footprints with locked door portals | the farming pillar needs private extendable land while presence on the shared map stays the social point; mines remain shared per docs/26 | docs/26 spaceId u8 and per-player instancing non-goal (mines-only now)
2026-08-26 | design/homesteads | crop growth and barrel curing are derived pure functions of planted/sealed ticks — zero writes while growing, offline progress free and full-rate, and nothing ever withers (unwatered or out-of-season crops slow or pause, never die) | the incremental loop must scale to many farms under the docs/34 no-tick-work law, and the docs/06 anti-frustration floor is binding | —
2026-08-26 | design/homesteads | homestead access is role-tiered (guest/worker/builder) behind owner-approved requests with audited revoke and kick, and worker harvests route to the homestead's shared output chest rather than the worker's inventory | friends-only co-op needs real permission boundaries without theft ambiguity; helping a farm is helping the farm | —
2026-08-26 | design/homesteads | farming v1 is homestead-only (seeds plant only inside farm instances); overworld planting is an explicit later hook | it keeps the gold loop tunable, the permission model airtight, and the overworld free of crop-row sprawl until the docs/30 world lands | —
2026-08-26 | backend/homesteads | all homesteads are logical `spaceId` coordinate worlds in one shared SpaceTimeDB database, not one database deployment per farm; leaving unsubscribes client state and stops occupancy-scoped simulation but does not unload authoritative tables, while generated terrain/collision caches must be bounded and evictable | one transactional authority keeps portal travel, inventory, permissions, moderation, migrations, and backups coherent; subscription scope is client interest management rather than server-memory lifecycle or authorization | per-homestead database deployments and unbounded per-space caches
2026-08-26 | design/progression | adopt docs/36: quality-of-life capabilities are earned through skill trees (participation itself is never gated), with the standing layering rule that skills unlock the option and gold buys the thing | the owner wants unlock-driven progression where sprinting, riding, pack space, and automation are all chosen and remembered, without ever locking players out of farming, crafting, or visiting friends | —
2026-08-26 | design/progression | three independent XP tracks (combat/explorer/farming) earned only by server-recorded actions, each levelling its own tree of ranked minors, verb keystones, and hidden nodes revealed by statistics thresholds or quests | per-track XP keeps every playstyle progressing and hidden nodes give the incremental there-is-more itch; kills now grant Combat XP in danger zones | docs/32 combat-grants-no-XP rule
2026-08-26 | design/progression | horse riding and horse jumping become Explorer keystones; existing characters receive a one-time Explorer XP grant sufficient to buy Horsemanship at migration | riding is the flagship QoL unlock, and grandfathering by grant preserves earned-feel without stripping current players mid-session | riding free since the horse landed
2026-08-26 | design/progression | quest objectives are sensed through the docs/33 statistics registry with accept-time baseline snapshots, and quest givers speak through the docs/31 dialogue registry | every countable objective works with zero per-quest instrumentation and no new conversation UI; the statistics contract becomes the quest engine's sensor array | —
2026-08-26 | design/progression | the character screen surfaces resolved attributes with per-source breakdowns, appearance changes are free and ungated, and no mechanical gender field exists — presentation comes from hair/outfit/skin axes on the single pack body | appearance is identity rather than progression, and the licensed pack ships one body silhouette; body-shape variants are original-art later work | docs/25 attributes hidden from the player
2026-08-26 | world/spaces migration | SpaceTimeDB 2.8.2 accepted the populated in-place reshape of all eleven spatial by_chunk indexes from [chunkX, chunkY] to [spaceId, chunkX, chunkY], together with defaulted u16 spaceId columns; the versioned-replacement fallback is not required | a dedicated scratch database was first populated from origin/main, then republished without deleting data; the migration plan removed/recreated only derived indexes and preserved the database identity | docs/26 §3 fallback
2026-08-26 | world/chat | deliver world-entry and final-disconnect messages through bounded private per-connection session inboxes, delete them on recipient logout, and purge legacy system rows from durable chat | presence is transport/session state rather than authored conversation; keeping it in General consumed history and replayed stale events after reconnect | persistent lifecycle rows in chat_message
2026-08-26 | design/lighting | add a tick-derived 29.5-day lunar month with all eight phases; Full Moon preserves the current clear-night ambient, New Moon reaches #141420, and the cycle never resets at the 28-day seasonal year | the owner wants moon phase to control how dark night becomes while remaining aligned with the shared day/season calendar; the 1.5-day drift lets every season eventually experience every phase with zero stored state or netcode | docs/27 universal 89/channel surface-night floor
2026-08-26 | world/growth | use one deterministic integer growth profile with composable water, fertilizer, poison, and biome basis-point modifiers; sparse overworld plants persist coarse progress while dense crops derive it from elapsed authority ticks | trees, cacti, crops, and later plants need one server-owned lifecycle without a per-crop tick-write scalability failure | ad-hoc tree-only and crop-only growth clocks
2026-08-26 | client/subscriptions | subscribe each regional table with one bounded rectangular query instead of one query per table per chunk | the fragmented set produced continuous SpaceTimeDB cache-refcount errors for 20 Hz NPC updates in the authenticated public client; rectangular bounds preserve neighborhood scoping while reducing settled queries from hundreds to 36 total | docs/34 stage-2 per-chunk regional query set
2026-08-26 | client/lighting | retain quarter-tile light texels, increase falloff from 10 to 16 bands, track open-light centers at one-world-pixel increments, and bilinear-filter only the composed light layer | live owner review found coarse light texels visibly terraced and carried lights snapped as the player moved; smoothing the overlay preserves crisp world art while the representative 40-light browser fixture remains within budget | docs/27 ten-band falloff and temporary two-texel implementation fallback
2026-08-26 | client/lighting | keep quarter-tile spatial sampling but use 8-bit strength, quarter-world-pixel fractional seeds, alpha-silhouette direct umbrae for solid props and terrain walls, and short feathered trunk-only ground shadows with receiver ownership/depth; emissive props never join their own occluder set and low collision/relief such as ponds and inset terrain is optically transparent | live owner review found the 16-band occluded flood still held for several player pixels, collision AABBs darkened transparent sprite padding, flood routing leaked behind rocks, full-umbra trunks produced grid-shaped corridors and self-shadowed their elevated tree sprite, while collision-only ponds and decorative dirt edges cast false shadows | docs/27 16-band falloff and 55% collision-footprint attenuators
2026-08-26 | design/movement | make Shift Sprint a baseline server-authoritative ability at 150% walking speed with slow Vigour drain; resolved Constitution improves efficiency and sprint speed/cost use the shared equipment/effect/skill/environment modifier pipeline | the owner wants sprint available now while retaining clean extension points for buffs, debuffs, and progression; queued intent plus shared prediction prevents Shift transitions from becoming a client-only speed exploit | docs/03 running costs nothing; docs/36 future sprint progression may improve the baseline rather than gate movement
2026-08-26 | design/movement | retune baseline Sprint to 125% walking speed and double its drain to 10 displayed Vigour per second at 10 CON | the owner wants Sprint to be a meaningful but modest burst rather than near-mount movement; the existing CON and modifier hooks remain unchanged | 2026-08-26 design/movement 150%-speed / 5-Vigour-per-second tuning
2026-08-26 | design/vitals | increase baseline Vigour regeneration by 50% to 12 displayed Vigour per second at 10 CON while keeping regeneration fully suppressed during active or queued Sprint consumption | recovery should feel less punitive without allowing simultaneous drain and regeneration to dilute an ability's configured cost | 8-Vigour-per-second baseline
2026-08-26 | client/interaction | resolve the contextual E prompt and E action through one nearest-distance candidate list across NPCs, chests, placeables, horses, portals, resources, and ground items; kind and stable id break exact ties only | nearby interaction ranges can overlap, so hard-coded entity-type precedence made a distant NPC steal interaction from the chest at the player's feet and could let prompt/action logic drift | hard-coded portal/placeable/campfire/merchant/horse/chest/pickup precedence
2026-08-26 | world/crafting | destructive dismantling returns recipe-derived component stacks plus stored contents instead of also returning the intact crafted object; chest destruction now yields its eight planks | deriving salvage from the shared recipe registry prevents balance drift, while returning both the object and all inputs would create an unlimited place/break material duplication loop | intact chest drop on the third axe hit
2026-08-26 | world/interaction | chest open and axe-break eligibility use the nearest chest in a shared two-tile radial reach on client and authority; hands pickup remains faced-tile | the legacy exact-facing gate made prompts disappear at close diagonal angles and could reappear after stepping farther away, while physical pickup should remain deliberate | exact faced tile for every chest action
2026-08-26 | terrain/elevation | model raised land as arbitrary nested integer contours with semantic crossings and elevation-aware 2.5D painter depth; keep logical level, visual face rows, and collision thickness independent | the owner requires cliffs inside cliffs, easy future raise/lower editing, lower actors hidden behind rear terrain, and upper actors visible on top regardless of selected cliff tileset | docs/30 former ambiguous “vertical terrain/3D” exclusion and foot-Y-only terrain rendering
2026-08-26 | terrain/elevation | ship deterministic three-level nested contours and one paired south-facing slope per connected contour on legacy island world version 26 before the infinite-world phases | the owner chose to make stacked terrain playable now; repeated insets reuse the future editor model, while stable-id resource reconciliation and wildlife regeneration keep the beta migration controlled | docs/30 §11 phase order where the legacy generator stays level-1 until later mountain phases
2026-08-26 | world/direction | adopt docs/40: the target shared overworld is a safe non-destructive sanctuary/travel layer; gathering, construction, farming, and opt-in danger live in typed `spaceId` destinations reached through symbolic-scale POIs | prevents depletion/griefing, keeps the shared map attractive, permits bounded reset policies, and makes Shared Spaces the common substrate for Homesteads, forests, caves, towns, events, and interiors | generally destructible survival overworld; literal-scale player estates; overworld combat
2026-08-26 | world/delivery | prove docs/40 with a transitional Shared Spaces technology demo before migrating the current island: test forest, cave, deeper underground, and a Marlow-sold deed that atomically places a compact tent POI leading to a durable owner Homestead | validates dynamic definitions, portals, isolation, subscriptions, persistence, and cache bounds before world-authoring/content work depends on them | flag-day sanctuary rewrite before spaces proof
2026-08-27 | design/worldgen | reactivate docs/30's signed seed-generated topside as a procedurally assisted safe sanctuary: untouched chunks are pure seed/version output, permanent admin edits are sparse semantic overrides, the v2 generator starts fresh, and the legacy island is retained as an optional stamp and regression fixture rather than mandatory production terrain | a Minecraft-style seed world supplies vast coherent terrain without hand-authoring every region, while doc 40's safe-world and destination-space rules prevent depletion and griefing; semantic generation before art guarantees reviewed shore, river, path, biome, and cliff transitions across chunk seams | docs/40 finite authored sanctuary and deferred vast extent; docs/30 §8 mandatory pixel-perfect whole-island migration
2026-08-27 | world/materialization | materialize only a radius-12 spawn-readiness square when a sanctuary version is created, then generate chunks through bounded player-interest look-ahead; keep immutable terrain seed-derived and persist only chunk lifecycle, deterministic entity state, and sparse curation | a 25×25-chunk centre covers the maximum client view plus preparation buffer without bulk-generating the approximately sixteen million possible chunks; absolute-coordinate sampling and topology halos make late neighbours seam-identical regardless of generation order | publish-time whole-world generation or per-tile terrain persistence
2026-08-27 | world/development | isolate all procedural-sanctuary authority experiments in the local `orchard-cellar-procedural-dev` database and leave `orchard-cellar-world` active and untouched until an explicit rehearsed cutover | generator/schema iteration must not regenerate or migrate the beta world's terrain, accounts, or bindings while the new seed world is incomplete | developing the procedural module against the current world database
2026-08-27 | design/ranged | render bow shots and their charge/range tracer from one shared parabola, rotating arrows along its tangent while authority continues swept ground-plane collision; future skill/stat range modifiers must feed the same shot budget | owner requested an arcing bow whose tracer truthfully previews eventual stat-driven distance without weakening server authority | docs/32 fixed-power and later bow-charge hook
2026-08-27 | design/ranged | let the cursor select the bow destination up to the authority-resolved maximum instead of increasing range with hold time; every fired arrow becomes a planted world pickup for 30 seconds and can be recovered only through the existing server-authorized nearby E transaction | direct destination selection makes the tracer an intentional aiming tool, while deterministic recovery preserves one consumed arrow per shot without trusting client ownership, range, timing, or inventory state | 2026-08-27 design/ranged charge/range tracer wording; docs/32 probabilistic terrain-only arrow retrieval
2026-08-27 | design/combat | permit exactly three authored non-hostile regenerating archery targets as a narrow sanctuary practice exception, backed by the reusable deterministic combat-damage and damageable-entity substrate | the owner requested a safe proving ground for selection, health, floating damage, embedded arrows, and carry placement before hostile content; limiting authority damage to registered target rows preserves the sanctuary's no-player/NPC/scenery-damage contract | docs/40 blanket sanctuary rejection of all damage; docs/32 danger-zone-only damage
2026-08-27 | balance/combat | make server-timed bow draw scale both range (16–240 px) and Vigour (1–30 over one second), with the client duration accepted only as a range-reducing cap; keep the full cursor-directed tracer stable and fill its reachable dots red as charge grows; render authority-confirmed critical damage in bold yellow | quick loose shots should be short and cheap without making the aiming guide jump, while full shots visibly drain substantial stamina and critical feedback must remain immediately legible without client-side rerolls | docs/06 fixed bow cost 10; 2026-08-27 design/ranged fixed cursor-selected range
2026-08-27 | operations/chat | make `/last` an owner-only on-demand view of the 12 newest retained connection-audit events, returned solely through the caller's bounded session notice inbox with UTC timestamps | connection history is useful for development and moderation but must not expose the private audit table, enter General, or survive the requesting transport session | public connection history or durable system chat messages
2026-08-27 | world/items | replace source-anchored cosmetic inventory dragging with a private server-authoritative per-player cursor stack and Minecraft-style PICKUP, QUICK_CRAFT, PICKUP_ALL, QUICK_MOVE, SWAP, and THROW transactions | persistent mouse pickup cannot be secure or behave consistently when the item remains usable in its source row; explicit cursor custody makes every gesture atomic, metadata-aware, reconnect-safe, and independent of mouse release | 2026-08-25 world/items shift-drag preview and docs/23 cosmetic DragContext
2026-08-27 | world/homesteads | ship closed-by-default owner-controlled Homestead gates, owner-only exterior mutation, and atomic mounted travel between sanctuary and Homestead exteriors while retaining `no_horses` for every indoor or underground destination | the owner needs a farm they can freely modify and deliberately open for visits without granting visitors destructive authority or allowing a portal transaction to orphan a ridden horse | permanent-open demo farms and blanket `no_horses` on Homestead entry; docs/35 role-tier access remains a later extension
2026-08-27 | world/quests | implement reusable private per-player quest state with accept-time baselines, server-derived statistic/inventory/action/location objectives, dialogue-controlled acceptance and turn-in, caller-private quest props, track-XP/currency/item rewards, and a collapsible HUD tracker | future quests need one server-authoritative definition and sensor contract rather than bespoke client flags; Marlow's private tent book proves conditional world items and rewards end to end | docs/36 quest phase's future-only status
2026-08-27 | world/migration | apply the additive Homestead gate, quest/skill/thought/private-prop, and world-surface schema non-destructively to the populated local `orchard-cellar-world`; retain the older anvil scratch unchanged after it correctly rejected an unrelated pre-existing placeable column reorder | the current populated schema is the relevant production-copy vehicle and migrated with defaults/views intact, while deleting or force-resetting an older incompatible scratch would not prove safe migration | destructive scratch reset or `--delete-data`
2026-08-28 | design/progression | ship the invented Combat, Explorer, and Farming tree topology, persistent ranks, resets, and owner debug point grants as a reviewable first slice while leaving all described skill effects and verb gates inactive | the owner explicitly requested skills that are not wired into gameplay yet; separating content/topology review from balance and reducer integration keeps the preview honest and avoids partially active capabilities | docs/36 phase 3's atomic tree-plus-effects delivery
2026-08-28 | world/progression migration | accept the populated in-place addition of `bonusPoints`/`respecCount` to skill tracks and the private `player_skill_node` table; regenerate affected caller-private views and bindings, with one expected client disconnect on publish | the local production-copy database accepted defaulted columns and the new table without deletion, while view shape changes are derived interface recreation rather than persistent-row loss | destructive reset or a versioned replacement table for this additive shape
2026-08-28 | design/farming | ship all 22 licensed crop groups with four-stage water-gated growth on tilled soil in both the overworld and owner Homesteads; Marlow stocks every seed packet, one watering supplies one 15-real-minute game day, and crops pause safely when dry | the owner explicitly expanded farming beyond the six-crop Homestead-only plan and wants long-running crops that require continued care without per-crop tick writes or withering | docs/35 six-crop accelerated-growth roster; docs/40 blanket overworld-farming prohibition; 2026-08-26 farming-v1 Homestead-only decision
2026-08-28 | design/farming | make empty overworld soil decay to its unchanged base terrain seven game days after its latest till, watering, or harvest, while planted plots and all Homestead soil remain permanent; enforce this with stale-safe one-shot timers | temporary wilderness farms should heal without threatening crops or a player's owned farm, and exact wake-ups preserve the zero-per-tick farming scalability rule | permanent empty overworld soil; global per-tick soil decay scans
2026-08-28 | design/smelting | compact nine pebbles into stone and nine iron/copper/gold pieces into one ore chunk, then smelt one chunk with one wood or plank over five real minutes; gems stay unsmelted, bars sell for twice ore-plus-wood value, and barrels require six planks plus two iron bars | the owner wants slow early progression, a durable mining sink, and a metal gate before crop barreling without inventing coal, silver, or bronze ore outside the existing authoritative material roster | docs/28 former 10-second two-ore smelts and seven-plank barrel
2026-09-02 | balance/commerce | raise the finished Bottle sell price from 1 silver 20 to 50 silver (5,000 bronze) while leaving Must, fruit, and station prices unchanged | the Bottle is the estate's signature product and the payoff of the whole press-and-age chain; at 120 bronze it barely beat the three Must it consumed, so the first-bottle loop had no visible reward step | 120-bronze Bottle inherited from the crafting phase
2026-09-03 | editor/content | OPEN: adopt doc 55 game authoring suite — content moves from packages/sim TypeScript literals to SpaceTimeDB content_definition rows (JSON payload per definition, one CAS head), behaviour becomes a bounded trigger/condition/effect graph interpreted by shared sim code, station windows become frame definitions; engine primitives still require an additive module republish | SpaceTimeDB 2.8.2 cannot add reducers at runtime, so live authoring must be data interpreted by fixed reducers; owner to confirm JSON-in-row vs typed columns and the content_editor grant | docs/02 sim-literal content assumption once implemented
2026-09-03 | editor/studio | OPEN: adopt doc 56 — move the editor out of packages/client into a separate packages/studio application (same monorepo, own origin, own release), extracting @orchard/engine, @orchard/ui, @orchard/world-bindings, and @orchard/auth as shared packages; add an audited administration API using SpaceTimeDB procedures for parameterized reads and admin* reducers with dryRun/reason/inverse for writes; add content_editor and support grants | views cannot take parameters and private tables cannot be filtered client-side, so player support needs procedures; a separate repository would drift from bindings and sim; owner to confirm hostname vs path and the support role | docs/02 repository layout once implemented
2026-09-03 | editor/studio | Studio is served at cellar.dastari.net; every editor surface moves into packages/studio including Character Studio and the audio preview; the game client keeps no /editor routes, editor.html, or audio-preview.html (proxy-level redirect only during transition) | owner decision; one origin per app keeps CSP, service worker, and OIDC clients separate and removes editor code from the game bundle | 2026-09-03 editor/studio OPEN hostname-vs-path and scope questions (support grant remains open)
2026-09-03 | editor/studio | the support grant (capped player remedies, no economy edits beyond balance-defined caps) ships in doc 56 Phase 2 alongside the administration API and Player Manager | owner decision; day-to-day player help must not require an owner or admin account | 2026-09-03 editor/studio OPEN support-grant question
2026-09-03 | editor/studio | Studio keeps the Map Editor visual language and the Teams-style side rail (improved with badges, chords, session card), adopts Godot-derived docks (World/Live Outliner, Inspector, Content Browser, bottom panel, layouts, tool registration API), and treats connecting to and editing the live world as the normal case for every tool | owner direction; one visual system across authoring and operations, familiar editor workflow for contributors, no separate admin-panel look | —
2026-09-03 | migration/data-preservation | docs 55 and 56 may take unlimited planned downtime, but must preserve the original world and all player-owned state so existing clients can rejoin and operate normally; every rollout requires verified backup/restore, non-destructive publish, state-parity checks, and reconnect acceptance before traffic returns | owner made world continuity and complete player-state preservation the hard invariant while explicitly allowing downtime | any migration or rollout optimized for availability at the expense of recoverability or state parity
2026-09-05 | lifecycle/approval | owner-only Tier-B approval via orchard-studio owner OIDC plus exact canonical bundle SHA-256 confirmation at release; replace detached approvals with an unsigned digest/owner-subject/timestamp record and remove the signature/keyring/expiry layer | solo project: agents author callbacks but never self-approve because the release confirmation is a human action; revisit if a second person joins; retain restricted AST, isolated builds, backup/restore, non-destructive publish and state parity | two-signature Tier-B lifecycle design

2026-09-05 | lifecycle/approval | Tier-B releases are approved by the owner verbally in chat for each specific live-world change, including explicit advance approval; agents run normal restricted-AST validation, typechecks, tests and world build, explain the change briefly, then execute the guarded release after “Go”; remove owner-confirmation records, digest-confirmation environment gates, candidate spools, handoff CLIs and the loopback build service | solo project: the owner’s chat approval is the control; revisit if a second person ever joins; approval never carries between releases and never waives verified fresh backups, rollback artifacts, non-destructive publish, state parity or reconnect checks | earlier 2026-09-05 owner-only OIDC plus digest-confirmation decision and two-signature design
2026-09-05 | operations/release-backups | routine code and UI updates rely on the owner's Proxmox backup policy; agents do not set up application backups or require a fresh application backup and restoration rehearsal for every routine update; preserve no-delete publication, checked code rollback and same-identity reconnect, while actual stored-data migrations retain fresh verified backup, isolated restoration rehearsal and state-parity gates | owner will manage host backups and wants routine live fixes to finish promptly during early development; this records responsibility, not a claim that Proxmox backups are configured or verified | the routine-update backup freshness requirement in the 2026-09-03 rollout invariant and 2026-09-05 lifecycle release policy; per-release chat approval and data preservation remain unchanged
2026-09-05 | client/releases | bump the app semantic version for every shipped UI version; use patch increments for compatible bug fixes, minor for compatible features, and major for breaking changes, keeping current workspace versions aligned | the owner needs distinguishable live UI releases and predictable refresh/version reporting | reusing the same displayed version across UI deployments
2026-09-05 | art/lighting | compile exact per-asset bakedShadowColor into validated lazy category spans while preserving the sole original sprite/PNG | owner requested one artwork source for future Dynamic omission and Basic baked shadows; 35 tree/stump declarations reviewed, all 36 original atlas PNGs remain identical, category v1/legacy readers remain compatible; runtime removal and seasonal shadows are not yet enabled | docs/58 M1 asset foundation
2026-09-05 | client/lighting | implement renderer-owned exact-shadow frame selection with an 8 MiB filtered-surface budget, visible-set pinning, generation cancellation and immutable original UI/art; keep public omission disabled until replacement celestial shadows land | one source sprite must serve Basic and Dynamic without per-frame readback or shadowless transition flashes; browser fixture removes exactly 285 oak pixels with zero changed body pixels | docs/58 M2 cache/plumbing, public rollout still pending
2026-09-05 | client/lighting | implement Basic/Dynamic as a persisted Video quality choice separate from the Developer Classic/Unified solver; migrate the disabled-effects preference to Basic, retain original baked shadows, use one world tint and bypass spatial-light producers, masks, solve and receiver correction | low-end mode must preserve night colour while releasing dynamic-only state; tint is composed before world presentation and HUD remains separate | legacy fullbright disabled-effects semantics; docs/47 public Classic presentation baseline for quality/ambient only
2026-09-05 | client/lighting | derive seasonal sunrise/sunset and celestial altitude/direction from the shared authority clock, with warm low sun, blue phase-dependent moonlight and cloud attenuation; feed the same unoccluded RGB to Basic and current Dynamic ambient | longer summer days, shorter winter days and an independent 29.5-day lunar cycle require one presentation clock; moving generated sun/moon shadows remain the next milestone | fixed-hour outdoor client ambient schedule; no authority state change
2026-09-05 | client/lighting | implement per-receiver RGB sky/local max resolve, clean-body finite sun/moon silhouettes, explicit contact footprints and separate local-light elevation fields in the renderer; gate public activation on gameplay painter/readiness/fallback acceptance | generated shadows must move with their own celestial source, preserve the blue moonlight, allow lantern fill and leave elevated artwork free of ground shadows | doc 58 M5 renderer core; full gameplay integration remains pending

2026-09-06 | client/lighting | publish seasonal receiver lighting as the Dynamic default in 0.5.0, with exact declared-pixel omission, independently shadowed sun/moon/local contributions and a Basic original-art fallback | explicit owner authorization to finish and deploy; 45 reviewed declarations, immutable atlas PNGs, generation-aware preparation and zero retained Basic lighting bytes verified; actor volume approximation, remaining asset candidates and measured Dynamic performance limits are documented in doc58 release evidence | public-activation gate in earlier doc58 checkpoints; unrelated doc47 performance work stays open
2026-09-06 | client/performance | adopt doc 59 client render performance recovery: attribution baseline on desktop and physical iPad first, then 1× world pass, bounded atlas pages, shadowless page variants, static/moving directional coverage split, shared tint pool, painter hot-path cleanup and frame pacing, each with recorded stage p95s | 0.5.0 evidence shows Basic 13.4 ms and Dynamic 21.5 ms p95 on desktop with no stage attribution; the 512×52032 character atlas, per-frame filtered/tint canvases, per-frame coverage rebuilds and device-resolution world pass are the identified causes | —
2026-09-06 | art/lighting | the atlas builder emits a per-page `.omit.png` variant with declared baked-shadow spans cleared; the client loads one variant per page for the active quality and the runtime filtered-frame cache is deleted | owner confirmed doc 59 decision A1: one authored asset, one asset ID and one source PNG remain, the variant is disposable build output like the spans; runtime per-frame filtering defeated draw batching and caused the 0.5.5/0.5.6 flicker fixes | docs/58 D1 “no extra dynamic/basic PNG exports” and D3 “compile the selection, not another image”
2026-09-06 | client/rendering | render the world pass at 1× world pixels by default with a two-stage nearest-then-smooth upscale; Video offers World scale 1×/2×/Native so device-pixel sprite motion can be restored | owner confirmed doc 59 decision B2; device-scale rendering multiplies fill rate by up to 36× on DPR-2 tablets and is the largest single cost | docs/21 ceil(zoom×dpr) integer world pass as the only mode
2026-09-06 | client/rendering | build a WebGL2 world pass as an experimental client-side Video toggle (“Experimental: WebGL renderer”), off by default and persisted per client; Canvas 2D remains the reference implementation, the default and the automatic fallback on any failure; HUD stays on Canvas; making WebGL2 the default requires doc 47 §15 adoption numbers and a further entry here | owner direction: players may opt in while measurements accrue instead of waiting for a Canvas budget failure; doc 47 §15 technical rules (capability seam first, nearest inputs, no readback, context loss, disposal, fallback) remain binding | docs/47 §15 “prototype WebGL2 only if optimized Canvas still violates the 4 ms budget” entry condition; doc 59 §3.2 C open question

2026-09-06 | tools/performance | exclude historical output/playwright evidence artifacts from repository lint; retain source and new perf-59 harness lint | doc59 preflight npm run check fails on 778 errors exclusively in historical generated bundles and probe scripts; required green-commit gate needs this bounded eslint.config.js scope exception | doc59 P0 file list extended for preflight only
2026-09-06 | client/performance | OPEN: doc59 P0 authenticated gameplay baseline is blocked after three access paths; existing orchard-cellar-dev connects but never supplies clock/environment to current bindings, fresh orchard-cellar-dev-perf59 rejects the local profile with authentication_invalid_issuer, canonical shared-browser sign-in reaches the credential form with no active OIDC session | keep authority/authentication unchanged; do not report loading-screen frames as gameplay; instrumentation can proceed, dependent milestones remain gated until P0 evidence exists | —
2026-09-06 | tests/performance | refresh three stale baseline test fixtures before the doc59 claim: preserve farmcraft's existing exact Cute Fantasy apple provenance, assert 0.5.6 synchronous frame streaming/reuse and real unavailable-surface failure, and pin the unchanged 0.5.7 structural seam digest | canonical npm run check passes 3403 tests but these four assertions still describe earlier accepted states; artwork and runtime remain unchanged, independent package-boundary checks remain active; per-file seam hashes match preservation snapshot 3cfbf395 | doc59 preflight scope exception for skill-icons.test.ts, phase-zero-extraction.test.ts and shadow-presentation.test.ts only
2026-09-06 | tests/assets | the preflight skill-icon provenance fixture explicitly checks all three existing derived icons (farmcraft/apple, mining_endurance/anvil, fishing_endurance/fishing rod), including approved source assets; native Clockwork Raven region checks remain for other icons | the first focused correction exposed two further existing derived assets after the farmcraft assertion; no source art is modified and no arbitrary provenance is accepted | extends the same doc59 preflight test-only correction

2026-09-06 | client/performance | Doc 59 P0 authenticated access resumed after the owner logged into the canonical game. The previous OPEN access entry no longer describes the account state. The candidate uses a private Tailscale HTTPS preview on port 5181 and the existing validated identity; no auth policy or live deployment changed. The shared preview currently reports `visible:false` and does not deliver active rAF, despite open/focus/resize attempts; screenshot automation also fails. OPEN: shared-browser visual/active-rAF qualification remains unavailable. Local real-client Playwright capture continues, explicitly recording Linux/Headless Chrome metadata | owner login changes the previous technical access condition; active-rAF and visual results must still be real | previous P0 unauthenticated-session OPEN entry, for account access only
2026-09-06 | client/performance | Doc 59 P0's required iPad one-button capture needs a bounded addition to `packages/ui/src/overworld-ui.ts`, beyond the milestone's initial file list: replace two disabled Render-panel placeholder rows with the capture action. Re-pin `phase-zero-extraction.test.ts` for this reviewed UI seam. Collection and clipboard behavior live in new modules; no gameplay or authentication authority changes. | minimum scope extension required by the owner's touch capture instruction | P0 file list, only for the Render-panel action and its structural seam test

2026-09-06 | client/performance | use the captured current 0.5.7 live stationary/walking scene as doc59 forward comparison baseline; retain 0.5.0 13.4/21.5 ms only as historical reference, and record the missing cap-performance workload qualification | the retained release has no camera/placement replay or caster/cap-run counts, so an exact 661-item reconstruction cannot be substantiated; new per-stage/counter samples and nested-terrain pixel goldens provide explicit evidence without fabricating missing content | doc59 P0 step 3 exact historical-scene reconstruction; targets and settled A1/B2/C unchanged

2026-09-06 | client/performance | Doc 59 P1 extends the P0 protocol metadata to report actual World scale, backend and active pixels, and corrects its walking period to four 625-ms legs so the 35-second protocol closes fourteen squares. Pin camera/sky through the existing presentation-only review seam for the controlled desktop matrix; retain the drifting initial matrix as unmatched visual evidence. Re-pin the UI structural seam for the new Video control | 500-ms legs left half a square after each sample and changed visible content between policies; capture labels and controlled comparisons must reflect actual settings and geometry | bounded P1 scope addition: gameplay-render-protocol.ts, render-protocol-walk.ts and phase-zero-extraction.test.ts

2026-09-06 | client/performance | Doc 59 P2 records atlas page dimensions/decoded bytes and explicitly labelled request-to-ready timings in the existing protocol export via a new shared page loader; desktop startup uses decoder-only Chromium trace event groups | the iPad owner needs page-limit and memory evidence from the existing one-button panel, without a second diagnostic workflow; current supplied registry writer is v2, so migration readers retain v1/v2/v3 and accept v4 | bounded P2 scope addition: gameplay-render-protocol.ts; no writer switch until dual readers are committed

| 2026-09-06 | client/performance | OPEN: doc 59 P4 step 3 constant-time receiver sampling violates the binding one-byte pixel gate. Three measured approaches on 76,800 deterministic full-moon receiver samples each: 4-px merged bilinear field (max RGB error 85); 1-px merged bilinear field (87); 1-px strongest/runner-up-owner bilinear field (50, also 50 without owner exclusion). Float64 fields exclude byte quantization as the cause; max composition and interpolation differ from the existing per-caster resolve, and a merged field needs explicit owner exclusion. Per docs/15 §9, stop this optimization after the three failures, retain the authoritative caster loop and continue independent static/moving field reuse and numeric keys. Do not claim O(1), a passed P4 pixel gate for that lookup, or full P4 completion. Reproducer and worst cases: `output/perf-59-20260906/P4/raster-accuracy-experiment.{ts,json}`. | codex |

| 2026-09-06 | client/performance | Doc 59 P2 startup exposed a second oversized decoded image after atlas paging: the client sign-in island backdrop is 1536×1024 / 6 MiB. Extend P2 minimally to `packages/ui/src/orchard-backdrop.ts` plus new backdrop page loader/builder/tests. Emit three 512×1024 generated tiles with original pixels and a revisioned manifest; the client composes the same cover crop from tiles. Preserve the original backdrop PNG for existing Keycloak/PWA build inputs. This is required by the literal no-decoded-image-over-4-MiB exit, and changes neither approved artwork nor world rendering decisions. Existing generated-resource service-worker discovery handles these tiles. | codex |

| 2026-09-06 | client/performance | OPEN: doc 59 P5 step 1 uniform five-bit receiver RGB quantization fails the binding one-step artwork golden. Three actual `runCelestialShadowReview` captures changing only the receiver-cache RGB input: nearest of 32 endpoint-inclusive levels (maximum channel error 4; 23,028 channels above one), five-bit truncation with bit replication (5; 25,852), truncation to multiples of eight (59; 65,540, including the 255-to-248 white fast-path change). The fixture makes 40 receiver calls with four distinct original RGB colours. Per docs/15 §9, stop uniform five-bit quantization after these three failures and retain exact RGB while continuing independent tint-page pooling and cap work. No pixel tolerance or A1/B2/C decision changes. These are accuracy captures, not stage timings. Evidence: `output/perf-59-20260906/P5/quantization-comparison.json`, `quantization-provenance.json`, scripts and PNGs alongside them. | codex |

| 2026-09-06 | client/performance | Doc 59 P3 removes the filtered-frame classes and therefore also updates UI index exports, the reviewed structural-seam digest, old engine cache tests and all three lighting review consumers. Historical review PNGs gain offline builder-produced omit inputs with identical frame tables. Existing PWA generated-prefix discovery already serves omit pages; no retention/content-addressing expansion is needed. | Required deletion/verification scope; docs 58 and 59 updated in the same change. |

| 2026-09-06 | client/performance | OPEN: authenticated doc 59 captures expired during P3 integration. Three existing-session recovery routes failed: canonical tab reload has no stored OIDC session; the unchanged production sign-in redirect reaches the identity provider's email/password form instead of completing SSO; neither field has a browser-provided value. No credentials, auth policy, authority data or live deployment were changed. Stop this recovery path pending a fresh owner sign-in; continue independent implementation and offline goldens, but do not relabel them as real-client measurements. Evidence: `output/perf-59-20260906/P3/authenticated-capture-block.json`. | codex |

| 2026-09-06 | client/performance | Doc 59 P4 minimally extends its file scope to `lighting-types.ts` for numeric owners, `lighting.ts` for a monotonic receiver-byte revision including reset, and `world-lighting-renderer.test.ts` for the changed split begin API. Add sampling/merge/upload durations to the existing seasonal stage wiring; the raster merge duration includes coverage work. Numeric feet preserve the previous rounded-coordinate equality, terrain owners remain in a disjoint weak identity namespace. The local revision is required to avoid stale cached RGB after local-light changes or reset; no world readback or changes to lighting colour are introduced. | codex |

| 2026-09-06 | client/performance | OPEN: doc 59 P5 dim-filter removal fails the one-step artwork gate after three offline CPU derivations over 33 real sprite/effect cases: a combined float brightness/saturation matrix clamped last, brightness clamped before saturation, and the latter with 0.88 opacity baked into alpha. Dimming still differs by two channel steps (72 / 72 / 1124 channels above one). The latter two match enemy and wildlife hit flash within one step; the first differs by 55 / 67. HUD witnesses remain exact. Stop this dim derivation after three failures; retain accurate rendering and continue independent tint-page work. No runtime readback or pixel-tolerance change. Evidence: `output/perf-59-20260906/P5/filter-feasibility/filter-comparison.json` and source/formula provenance alongside it. | codex |


| 2026-09-06 | client/performance | Doc 59 P6 scope amendment: include the authored-map sprite closure in `live-map-runtime.ts` and its reviewed `phase-zero-extraction.test.ts` source digest. These sprites run inside the gameplay painter and must share its single native state pair. Keep exact fractional depth and lexical tie order through the legacy producer adapter; numeric hashes identify items but do not reorder hash collisions. | Settled implementation scope; docs/59 updated in the same change. |

| 2026-09-06 | client/rendering | Doc 59 P8 Canvas seam minimally extends its file scope to world-pass-present.ts for explicit backing disposal and partial-constructor cleanup. The extracted backend retains the existing compatibility context while exposing sprite/chunk/cap/plane/weather/particle capabilities; Canvas sprite commands consume the current exact prepared source alongside immutable artwork/RGB/variant metadata for the later GPU path. No WebGL implementation or Video toggle is shipped by this extraction. | Required lifecycle seam; doc59 updated in the same commit. |

2026-09-06 | client/performance | P7 uses absolute 30 Hz presentation deadlines on every refresh rate, retains skipped-rAF simulation/counter work until the next submitted frame, and records the cap in capture metadata; scope includes metrics.ts, render-operation-counters.ts, gameplay-renderer.ts and gameplay-render-protocol.ts plus the mechanical gameplay-loop extraction | Every-other-rAF would be 60/72 Hz on 120/144 Hz displays; resetting protocol totals on skipped submissions would hide update work. Compact Video hides informational placeholder rows to preserve control/footer bounds. iPad default stays off until physical baseline qualifies opt-in | doc59 P7 implementation clarification

2026-09-06 | client/performance | P7 caches status/watch, currency and hotbar as three ordered native-display-coordinate layers, preserving live UI callbacks and explicit display-owner disposal | Tight cropping changed nearest edge sampling by up to235 channel steps; three original-size backings give45 exact real-art comparisons at fractional DPR. The retained HUD cost is12 bytes/display pixel; one flattened HUD cache would freeze live callbacks and alter ordering. HUD diagnostic counters are excluded from display-configuration equality | doc59 P7 HUD implementation detail

2026-09-06 | client/rendering | OPEN: P8 translucent GPU lighting fails the one-byte Canvas contract after three approaches: round/round tint, floor/floor, and rounded multiply with floor mask/quantized item alpha | Sprite channels above one were4144,3064,840 (maximum2 each); corrected ground residuals were568 then160 (maximum2). Attempt1 also had a separate ground-coordinate bug, maximum156, retained in evidence. Stop approximation attempts; production rejects unverified receiver/ground/multiply operations and uses the visible session Canvas fallback. Only internal comparison fixtures enable the unverified shader path | doc59 P8 lighting parity remains incomplete; output/perf-59-20260906/P8/webgl/attempt-{1,2,3}.json
2026-09-06 | client/rendering | P8 retains Canvas-copy presentation and rejects the CSS world-layer candidate | Matched software-GPU fixture composite p95 is7.1ms vs0.5ms, but Canvas-copy presentation is exact and CSS pixelated differs by up to82 channel steps (194053 channels above one) at427x240 to1280x720. This is SwiftShader diagnostic evidence, not hardware gameplay/adoption evidence | doc59 P8 presentation choice

2026-09-06 | client/rendering | P8 integration scope includes extracted gameplay renderer/fallback/protocol policy, receiver/ground source adapters, raw-field scene access, atlas cohort release, metrics and Video-row helpers | GPU submission must carry immutable artwork/RGB/raw coverage without CPU tint or scratch surfaces; default remains Canvas1× and the persisted experimental preference never overrides a session failure | docs/59 P8 scope extension; no dependencies or server schema changes

2026-09-06 | client/rendering | P8 OPEN parity qualification: literal existing-fixture replay also fails Celestial diagnostic GPU (max63,229351 channels above one); World/terrain reject ground-composite guard and legacy Lighting rejects filter | The prior three shader approaches already failed the binding tolerance; this expands the measured unresolved scope without another approximation. Seasonal diagnostic success and exact Canvas labels are not a full GPU HUD/parity pass | docs/59 ledger and output/perf-59-20260906/P8/webgl/all-fixtures/

2026-09-06 | client/rendering | P8 release packaging extends scope to client Vite grouping and the existing build-chunk gate | The first production build eagerly grouped WebGL implementation with Canvas despite the dynamic import. An isolated group without recursive dependency capture plus static-import traversal preserves the off-default backend's lazy code boundary; this is required release correctness, with no dependency changes | docs/59 P8 final packaging checkpoint

2026-09-06 | tooling | Restore the nested world TypeScript lock metadata to its resolved and installed5.6.3 while bumping only workspace/root versions to0.6.0 | The prior lock field incorrectly said0.5.7 and a broad release bump propagated it; tarball, integrity and dependency declaration are unchanged. No compiler upgrade or new dependency | docs/59 release lockfile correction
2026-09-06 | client/performance | doc 59 §8 amendments after the first pass: pin the benchmark workload (A-1); "zero lighting work" binds Basic only, Classic keeps its live lightmap (A-2); receiver sampling uses a spatial index feeding the unchanged per-caster loop, bit-identical, no O(1) claim (A-3); tint keys stay exact RGB on tint pages (A-4); dim/hit-flash CPU variants gate at two steps against the Chromium filter (A-5); cap runs stay in painter order with per-run tint caching (A-6); add P6b retained painter commands targeting painterBuild ≤ 2 ms (A-7); WebGL2 parity splits into an experimental-enable gate (max two steps, ≤0.5 % above one, HUD exact; the max-63 celestial and max-138 downsampling residuals are bugs to fix) and the unchanged one-step default-on gate (A-8); software-GPU timings are functional evidence only, hardware GPU and iPad rows are owner-run (A-9) | the P4 merged field, P5 five-bit quantisation, P5 dim derivation and P8 shader rounding each failed three measured approaches against gates this plan set; the failures show the gates were stricter than the visual contract (Canvas output already varies by one to two steps across browsers) or the approach changed the reference maths; painterBuild at 5.1 ms p95 is now the dominant measured cost in every mode; A1/B2/C are unchanged | the four 2026-09-06 client/performance and client/rendering OPEN rows for P4 sampling, P5 quantisation, P5 dim filters and P8 parity, which now have decided paths; the kickoff prompt's "Basic and Classic do zero lighting work" wording

2026-09-07 | client/rendering | Apply the bounded mutable-Canvas upload correctness fix under the existing P8 claim before the pending authenticated performance follow-up; keep all accuracy guards and the remaining follow-up order | Offline source-mutation replay proves stale pixels within one frame, while authenticated repeatability capture is unavailable; no performance or release gate is waived | Doc 59 follow-up sequencing for this correction only

2026-09-07 | release/client | Owner authorizes deployment of the completed 0.6.0 client after remaining doc 59 fixes and release gates pass, with exact candidate, rollback and commands recorded first | Owner instruction: "please continue with fixes then deploy once complete"; continue autonomously within the static-client scope | Initial doc 59 run stop-before-deployment boundary and separate final Go requirement for this client release

2026-09-07 | client/performance | A-1 capture instrumentation may also touch the existing gameplay-painter.ts queue seam and existing gameplay protocol modules, alongside new bounded modules and the P0 receiver diagnostics; main API wiring follows mechanical extraction 524c3d1e. The observer is optional and does not change sorting or drawing. | Actual projected player/pond commands and cap draw calls are needed to reject the offscreen-player and empty-cap workload recorded by earlier runs. | docs/59 P0 file scope only; rendering invariants unchanged

2026-09-08 | ui/kit | Unify Studio route rails and control rendering: transparent input centres, centred button captions, sentence-case field captions, muted disabled controls, shared icon/text contrast, authored focus outlines, native tiled skin composition and device-resolution SVG symbols | Owner reported inconsistent navigation, seams, blurry icons, mismatched colours and dropdowns closed by pointer-release rebuilds; preserve keyboard and clipping invariants | Ad hoc lab rail, opaque input fills and generic rectangular pointer-focus overlays
